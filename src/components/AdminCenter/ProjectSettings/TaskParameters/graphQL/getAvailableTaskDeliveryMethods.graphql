query GetAvailableTaskDeliveryMethods($parent: ID!){
    availableTaskDeliveryMethods(parent: $parent){
        available{
            id, name, created
        },
        alreadyExists{
            id, name, created
        }
    }
}